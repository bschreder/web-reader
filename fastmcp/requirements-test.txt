# FastMCP Server Test Dependencies
# ============================================================================
# These dependencies are only needed for running tests, not for production runtime.
# This keeps Docker images smaller by excluding test frameworks from the runtime image.

# Testing frameworks
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
pytest-mock>=3.12.0
pytest-timeout>=2.2.0

# Code quality
ruff>=0.8.0  # Fast Python linter and formatter
